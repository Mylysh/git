#include <iostream>
using namespace std;

int main (){
    double length, width; 
    cout << "*********Calculate area of a rectangle*********" << endl;
    cout << "Enter the length of the rectangle (cm): ";
    cin >> length;
    cout << "Enter the width of the rectangle (cm): ";
    cin >> width;

    cout << "The length of the square is " << length*width << "cm" << endl;
    return 0; 

}